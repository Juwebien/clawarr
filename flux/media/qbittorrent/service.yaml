apiVersion: v1
kind: Service
metadata:
  name: qbittorrent
  namespace: clawarr
spec:
  type: ClusterIP
  selector:
    app: qbittorrent
  ports:
    - name: webui
      port: 8080
      targetPort: 8080

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - base/
  - media/
  - agent/
  - config/
  # networking/ is NOT included by default — added by installer when DOMAIN is set

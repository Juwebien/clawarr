apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Only deployed when DOMAIN is set in clawarr-user-config
resources:
  - envoy-gateway.yaml
  - cluster-issuer.yaml
  - httproutes.yaml
